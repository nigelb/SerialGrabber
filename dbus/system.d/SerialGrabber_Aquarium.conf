<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>

  <policy context="default">
    <allow own="serial_grabber.Aquarium"/>

    <allow send_destination="serial_grabber.Aquarium"/>
    <allow receive_sender="serial_grabber.Aquarium"/>

    <allow send_path="/serial_grabber/Aquarium"/>
  </policy>

</busconfig>
